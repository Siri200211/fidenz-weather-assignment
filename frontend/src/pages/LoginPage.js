/* import { useAuth0 } from "@auth0/auth0-react";

function LoginPage() {
  const { loginWithRedirect } = useAuth0();

  const handleLogin = () => {
    loginWithRedirect({
      appState: {
        returnTo: "/dashboard"
      }
    });
  };

  return (
    <div style={{ padding: "40px" }}>
      <h2>Fidenz Weather App</h2>
      <button onClick={handleLogin}>
        Login
      </button>
    </div>
  );
}

export default LoginPage; */
